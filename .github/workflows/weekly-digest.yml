name: Weekly arXiv Slack Digest

# 週次ダイジェスト(完全クラウド)。
# GitHub Actions 上で claude -p をサブスク枠(CLAUDE_CODE_OAUTH_TOKEN)で実行するため、
# API従量課金は発生しない。arXiv取得・Slack投稿もランナーから直接行う。

on:
  schedule:
    # JST 月曜 08:00 = UTC 日曜 23:00
    - cron: "0 23 * * 0"
  workflow_dispatch:
    inputs:
      relevance_threshold:
        description: "この実行だけ通知閾値を上書きする(空欄なら通常設定を使用)"
        required: false
        default: ""

permissions:
  contents: write

concurrency:
  group: weekly-digest
  cancel-in-progress: false

jobs:
  digest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Python deps
        run: pip install requests feedparser

      - name: Install Claude Code CLI
        run: |
          curl -fsSL https://claude.ai/install.sh | bash
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"

      - name: Run weekly digest
        env:
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}
          SLACK_CHANNEL_ID: ${{ secrets.SLACK_CHANNEL_ID }}
          ARXIV_LOOKBACK_DAYS: "8"
          ARXIV_MAX_RESULTS: "500"
          DIGEST_CLAUDE_MODEL: "sonnet"
          # 通知閾値。手動実行の入力 > リポジトリ変数 > 既定値8 の順に優先。
          # 恒久的に変えるときは Settings → Secrets and variables → Actions →
          # Variables の RELEVANCE_THRESHOLD を編集する(コード変更不要)。
          RELEVANCE_THRESHOLD: ${{ inputs.relevance_threshold || vars.RELEVANCE_THRESHOLD || '8' }}
        run: |
          python scripts/fetch_candidates.py
          python scripts/judge_with_claude.py
          python scripts/post_slack_digest.py
          python scripts/finalize_weekly.py

      - name: Commit updated state
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/sent_ids.json data/candidates.json
          [ -f data/slack_threads.json ] && git add data/slack_threads.json
          git diff --quiet --cached || git commit -m "chore: weekly digest $(date +%F) [skip ci]"
          git push
